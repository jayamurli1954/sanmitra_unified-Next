# Test / development dependencies — NOT installed on Render.
# Run `pip install -r requirements-dev.txt` locally or in CI only.

-r requirements.txt

pytest==7.4.4
pytest-asyncio==0.23.3

# Panchang social card (HTML → PNG via Chromium)
jinja2>=3.1.0
playwright>=1.40.0
