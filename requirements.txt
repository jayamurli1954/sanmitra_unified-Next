# Production runtime dependencies.
# Versions pinned to what is tested locally — pinning makes pip resolution
# deterministic and lets Render's pip cache reuse wheels across builds.
# Test-only deps (pytest, pytest-asyncio) live in requirements-dev.txt.

# FastAPI 0.109.1 patches CVE-2024-24762. Coordinated FastAPI + Starlette 0.36+
# + httpx>=0.28 remains a follow-up: httpx 0.28 dropped Client(app=...) that
# Starlette 0.35 TestClient still needs.
fastapi==0.109.1
uvicorn[standard]==0.41.0

# Database drivers
pymongo==4.6.3
motor==3.3.2
sqlalchemy==2.0.25
asyncpg==0.29.0
alembic==1.18.3
psycopg[binary]==3.3.3

# Auth / crypto
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
python-jose[cryptography]==3.4.0
cryptography==50.0.0
google-auth==2.45.0
# pyasn1 0.6.4 is blocked: python-jose 3.4.0 requires pyasn1>=0.4.1,<0.5.0.

# HTTP / multipart
# httpx 0.28 removed the Client(app=...) kwarg that starlette 0.35's
# TestClient depends on. FastAPI 0.109.x still pins starlette 0.35.x, so we
# stay on httpx<0.28 until the coordinated FastAPI upgrade.
httpx==0.27.2
requests==2.33.0
python-multipart==0.0.31
defusedxml==0.7.1

# Config / validation
python-dotenv==1.2.2
pydantic[email]==2.12.5
pydantic-settings==2.14.2

# Documents / files
reportlab==4.0.9
weasyprint==68.0
pydyf==0.11.0
tinyhtml5==2.1.0
pypdf==6.16.1
Pillow==12.3.0
openpyxl==3.1.2

# Astronomy (Panchang)
pyswisseph==2.10.3.2

# Rate limiting
slowapi==0.1.9

# Web search (LegalMitra)
tavily-python==0.7.23

# Web Push Notifications
pywebpush==1.14.0
