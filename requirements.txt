# Production runtime dependencies.
# Versions pinned to what is tested locally — pinning makes pip resolution
# deterministic and lets Render's pip cache reuse wheels across builds.
# Test-only deps (pytest, pytest-asyncio) live in requirements-dev.txt.

fastapi==0.109.0
uvicorn[standard]==0.41.0

# Database drivers
pymongo==4.6.1
motor==3.3.2
sqlalchemy==2.0.25
asyncpg==0.29.0
alembic==1.18.3
psycopg[binary]==3.3.3

# Auth / crypto
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
python-jose[cryptography]==3.4.0
cryptography==46.0.5
google-auth==2.45.0

# HTTP / multipart
# httpx 0.28 removed the Client(app=...) kwarg that starlette 0.35's
# TestClient depends on. FastAPI 0.109 pins starlette 0.35.x, so we must
# stay on httpx<0.28 until FastAPI is upgraded.
httpx==0.27.2
requests==2.31.0
python-multipart==0.0.27

# Config / validation
python-dotenv==1.0.1
pydantic[email]==2.12.5
pydantic-settings==2.12.0

# Documents / files
reportlab==4.0.9
weasyprint==68.0
pydyf==0.11.0
tinyhtml5==2.1.0
pypdf==6.7.2
openpyxl==3.1.2

# Astronomy (Panchang)
pyswisseph==2.10.3.2

# Rate limiting
slowapi==0.1.9

# Web search (LegalMitra)
tavily-python==0.7.23
